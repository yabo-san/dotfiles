setup
# age private key — never deployed, never managed
.config/age/keys.txt
# entry script — fetched + run BEFORE chezmoi exists; never deployed to ~
bootstrap.ps1

# python bytecode — never deploy, never track
**/__pycache__/**
.config/scripts/hydra/node_modules/**
.config/scripts/hydra/package-lock.json
**/*.pyc

# ── OS gating: each platform only applies its own files ──────────────────
# Patterns below match TARGET paths (post dot_/private_ rename), not source names.
# On Windows, skip all the mac-only configs:
{{ if eq .chezmoi.os "windows" }}
Brewfile
.bashrc
.zshrc
.tmux.conf
.config/aerospace/**
.config/flashspace/**
.config/ghostty/**
.config/karabiner/**
.config/mise/**
.config/sketchybar/**
.chezmoiexternals/**
.chezmoiscripts/**
setup-alttab.sh
{{ end }}

# On mac/Linux, skip all the Windows-only configs:
# NOTE: for scripts, chezmoi strips the run_onchange_/run_once_/before_/after_
# prefixes when matching ignore patterns against the computed target name --
# same idea as dot_/private_ stripping for regular files. Listing the full
# source-prefixed name here never matched anything; these scripts were
# silently NOT ignored on non-Windows this whole time (confirmed: reproduced
# `install-obsidian-sync.ps1: exec format error` on a clean Linux box,
# 2026-08-18, independent of DevPod).
{{ if ne .chezmoi.os "windows" }}
playnite-scripts.ps1
startup-shortcuts.ps1
powershell-profile-bridge.ps1
install-rohankar.ps1
install-obsidian-sync.ps1
.glzr/**
.config/wezterm/**
.config/yasb/**
.config/scripts/**
.config/powershell/**
.config/starship.toml
.config/bootstrap/**
.config/fastfetch/**
.config/obsidian/**
{{ end }}
# NOTE: .config/bat IS shared (mac + Windows) — the preferred bat style.

# launchd agents are macOS-only (e.g. always-on backend services)
{{ if ne .chezmoi.os "darwin" }}
Library/**
{{ end }}

# nvim is shared (LazyVim overrides apply on both) — never ignored.




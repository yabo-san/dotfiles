setup
# entry script — fetched + run BEFORE chezmoi exists; never deployed to ~
bootstrap.ps1

# python bytecode — never deploy, never track
**/__pycache__/**
**/*.pyc

# ── OS gating: each platform only applies its own files ──────────────────
# Patterns below match TARGET paths (post dot_/private_ rename), not source names.
# On Windows, skip all the mac-only configs:
{{ if eq .chezmoi.os "windows" }}
Brewfile
.bashrc
.zshrc
.tmux.conf
.config/aerospace/**
.config/flashspace/**
.config/ghostty/**
.config/karabiner/**
.config/mise/**
.config/sketchybar/**
.chezmoiexternals/**
.chezmoiscripts/**
setup-alttab.sh
{{ end }}

# On mac/Linux, skip all the Windows-only configs:
{{ if ne .chezmoi.os "windows" }}
run_onchange_after_playnite-scripts.ps1
run_onchange_after_startup-shortcuts.ps1
run_onchange_after_powershell-profile-bridge.ps1
.glzr/**
.config/wezterm/**
.config/yasb/**
.config/scripts/**
.config/powershell/**
.config/starship.toml
.config/bootstrap/**
.config/fastfetch/**
.config/obsidian/**
{{ end }}
# NOTE: .config/bat IS shared (mac + Windows) — the preferred bat style.

# launchd agents are macOS-only (lumo-tamer always-on backend)
{{ if ne .chezmoi.os "darwin" }}
Library/**
{{ end }}

# nvim is shared (LazyVim overrides apply on both) — never ignored.


# bat config (Windows). Mac has NO bat config (pure defaults), so match that —
# default style (numbers + grid + header) and default theme (Monokai Extended).
# ONLY deviation: never page, so `cat` prints+returns instead of hanging in less.
--paging=never

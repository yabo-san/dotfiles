# bat config — shared mac + Windows (the preferred look over bat's defaults).
--paging=never
# clean style: line numbers + git changes + header (no heavy grid)
--style=numbers,changes,header
# ansi theme adapts to the terminal's Catppuccin palette on both OSes
--theme=ansi
